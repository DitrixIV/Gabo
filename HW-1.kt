fun main(){
    var a: Int = 4
    var b: Int = 8
    println("a + b equals " + (a + b))
    println("a - b equals " + (a - b))
    println("a * b equals " + (a * b))
    println("a / b equals " + (a / b))
    println("a % b equals " + (a % b))

}

